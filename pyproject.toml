[project]
name = "cv"
version = "0.1.0"
description = "Static site and LaTeX CV generator"
requires-python = ">=3.11"
dependencies = [
    "click>=8.3.2,<9",
    "jinja2>=3.1.6,<4",
    "pyyaml>=6.0.3,<7",
]

[tool.uv]
package = false
